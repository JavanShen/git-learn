name: CI
on:
  push:
    branches: main
  pull_request:
    branches: 
      - main
    paths:
      - '.github/workflows/ci.yml'
jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout respository
        uses: actions/checkout@v3